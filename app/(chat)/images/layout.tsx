import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Image",
  description: "Generate high-resolution AI visuals with FLUX 1 Schnell.",
};

export { default } from "./page";
